# Database 
database:
  type: MYSQL # SQLITE, MYSQL
  mysql:
    host: localhost
    port: 3306
    database: auroramc
    username: admin
    password: pass
    ssl: false
  
# Modules
modules:
  # Enable or disable modules
  customcraft: true
  duel: false